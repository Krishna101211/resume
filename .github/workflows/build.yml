name: Build Resume PDFs

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build_latex:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install TeX Live
        run: |
          sudo apt-get update
          sudo apt-get install -y texlive-latex-extra texlive-fonts-extra texlive-fonts-recommended texlive-latex-recommended

      - name: Find and compile all .tex files
        run: |
          for tex in $(find . -name '*.tex' -not -path './.git/*'); do
            dir=$(dirname "$tex")
            base=$(basename "$tex" .tex)
            echo "Compiling: $tex"
            cd "$dir"
            pdflatex -interaction=nonstopmode -file-line-error "$base.tex"
            pdflatex -interaction=nonstopmode -file-line-error "$base.tex"
            cd - > /dev/null
          done

      - name: Commit PDFs
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add '*.pdf'
          git add '**/*.pdf'
          git commit -m "Update PDFs" || echo "No changes"
          git push
